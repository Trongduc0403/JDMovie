@model IEnumerable<JDMovie.Models.Gioithieu>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Noidung)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sdtlien)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Noidung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sdtlien)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idgioitin">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idgioitin">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idgioitin">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
